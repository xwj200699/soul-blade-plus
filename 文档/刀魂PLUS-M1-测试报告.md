# 刀魂PLUS M1 · 测试报告

按 M1 Prompt 第十二节要求，严格区分三档验证等级。测试环境：node v22 无头运行**真实引擎**（DOM/Canvas/Image 打桩，Image 桩读取真实 PNG 尺寸），非代码走读。

---

## A. 已实际运行通过（无头引擎执行验证）

### 资源（P0）
| 项 | 结果 |
|---|---|
| 25 个 script 块全部语法解析 | ✅ |
| 内嵌资源 110 项，全部 data: URL | ✅ |
| 26 个 `_loadImg` 真实请求形态（`/assets/ui-lab/*.webp`）经成品内 `resolveEmbeddedAsset` 逐一命中 | ✅ 0 未命中 |
| 62 张角色精灵表路径全部命中 | ✅ 0 未命中 |
| fx/bgm/字体/背景命中 | ✅ |
| 解析器边界：头部斜杠 / 完整 URL / query+hash / `./` 前缀 / webp↔png 双向 / 已删变体拒绝 / 外部域拒绝 | ✅ 6/6 |
| ui-lab png 双重内嵌检查 | ✅ 0 张 |

### 战斗与系统（无头驱动真引擎 update()）
| 项 | 结果 |
|---|---|
| 引擎完整 boot（含 UI 资产链回退） | ✅ → title |
| 公平 AI 源级断言（cheatRead/readP/meterRegen/o.pad 读取 = 0 处） | ✅ |
| hard 反应窗口 = [5, 9] 帧 | ✅ |
| 三新角色全部动画表加载 + 帧数达标 + 真蹲姿（Crouch.png）接线 | ✅ |
| 六人扩展头像（sel+hud × 4 新角色）加载入 UI 资产链 | ✅ 2,2,2,2 |
| 悟空超杀 `staff` 完整分镜：命中→100 血打至 58→序列清零→自身落地 | ✅ |
| 后羿超杀 `arrowrain` 同上（→58） | ✅ |
| 安琪拉超杀 `flame` 同上（→57） | ✅ |
| LOCAL VS 全流程：标题四菜单 → P1 选角 → P2 方向键+小键盘选角 → 场景 → 开战，localvs 旗标就位、P1 无 AI | ✅ |
| P2 小键盘攻击命中 P1（100→94）、P2 方向键行走（560→544） | ✅ |
| hard AI-vs-AI 完整对局打完出胜者（3600 tick，最大连击 9） | ✅ |
| VS CPU 旧流程回归（选角→场景→难度→开战，localvs=false） | ✅ |
| 悟空/后羿/安琪拉必杀命中、后羿箭矢投射物、綾突刺（M0 冒烟沿用例） | ✅（M0 轮已验，M1 未改此逻辑） |

### 美术 QA（程序化断言）
| 项 | 结果 |
|---|---|
| 三角色 11 套动画帧：身高 77-86px（对齐前三名区间）、逐帧位移存在、攻击帧纯白月牙 400+ 像素（引擎 smear 管线可提取） | ✅ |
| 色彩层次：28-45 色/角色（v1 火柴人约 15 色） | ✅ |
| ASCII 轮廓抽查：悟空猴脸金箍/伸长棍、安琪拉双马尾裙装可读 | ✅ |

## B. 仅完成代码检查（未运行验证）

- 音量快捷键（-/= 9/0）：接线与夹取逻辑经代码审查，无头环境无 AudioContext 无法验听
- 失焦自动暂停：监听器注册确认，无头无 blur 事件源
- 暂停菜单按键不串流：原版 `Input.consume` 单次消费语义走读确认，未实测连按
- 结算页新角色立绘版式（套 mack 坐标）：渲染坐标代码审查，视觉效果未见
- CRT 壳/选人页排版在真实浏览器的观感

## C. 尚未验证 / 已知缺口

- **真实浏览器 file:// 双击全流程**（最重要的一项人工验收）：无头环境无法替代，需真人打开 `刀魂PLUS-M1-优化版.html` 过一遍 13.4 清单的截图项
- 手柄支持：未实现（见改动说明·未完成）
- 三场景动态元素：未实现
- 六人 × 13 项动作矩阵的逐项人工体感（无头只验证了判定与数值链路，动画观感需人工）
- 双人同机实战手感（按键冲突、win 键位布局差异）

## 结论

P0 资源失效、公平 AI、专属超杀、本地双人、六人头像五项硬指标均达成并经无头引擎验证；美术重做达成可量化指标（比例/层次/蹲姿/月牙），观感终审留待真人。按 Prompt 第十一节要求，本版本**不标记 final**，标记为 M1-优化版。
